<UserControl x:Class="SystemOptimizer.Views.DuplicateFinderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource SectionHeader}"
                   Text="Find duplicate files by content (SHA-256)" />

        <!-- Inputs -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Scan paths (; separated):"
                       VerticalAlignment="Center" Margin="0,4,10,4" />
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding ScanPaths}" Margin="0,4" />

            <CheckBox Grid.Row="1" Grid.Column="1" Content="Photos only"
                      IsChecked="{Binding PhotosOnly}" Margin="0,4" VerticalAlignment="Center" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Extensions:"
                       VerticalAlignment="Center" Margin="0,4,10,4" />
            <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Extensions}" Margin="0,4"
                     IsEnabled="{Binding PhotosOnly}" />

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Quarantine folder:"
                       VerticalAlignment="Center" Margin="0,4,10,4" />
            <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding QuarantinePath}" Margin="0,4" />
        </Grid>

        <!-- Results -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Results}" AutoGenerateColumns="False"
                  IsReadOnly="False" CanUserAddRows="False" HeadersVisibility="Column"
                  GridLinesVisibility="Horizontal" RowHeight="26"
                  BorderBrush="{StaticResource BorderBrushSoft}">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Remove" Binding="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" Width="60" />
                <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="90" IsReadOnly="True" />
                <DataGridTextColumn Header="File" Binding="{Binding FullName}" Width="*" IsReadOnly="True" />
                <DataGridTextColumn Header="Size" Binding="{Binding SizeDisplay}" Width="90" IsReadOnly="True" />
                <DataGridTextColumn Header="Modified" Binding="{Binding LastModified, StringFormat=yyyy-MM-dd}" Width="100" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" Text="{Binding Summary}" FontWeight="SemiBold" Margin="0,10,0,6" />

        <!-- Actions + status -->
        <Grid Grid.Row="4">
            <StackPanel Orientation="Horizontal">
                <Button Content="Scan" Command="{Binding ScanCommand}" />
                <Button Content="Quarantine selected" Command="{Binding QuarantineCommand}" />
                <Button Content="Cancel" Command="{Binding CancelCommand}"
                        Style="{StaticResource SecondaryButton}" />
            </StackPanel>
            <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center">
                <ProgressBar Height="6" Width="240" Value="{Binding Progress}" Maximum="100"
                             Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}" />
                <TextBlock Text="{Binding Status}" Foreground="#6B7280" Margin="0,4,0,0"
                           HorizontalAlignment="Right" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
